plugins {
    alias(libs.plugins.kotlin.jvm)
}

description = "JUnit integration for the OpenAPI validator project."

dependencies {
    api(project(":core"))

    implementation(libs.junit.jupiter.engine)
}